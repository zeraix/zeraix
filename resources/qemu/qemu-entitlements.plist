<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- HVF (Hypervisor.framework) requires this entitlement; matches homebrew's qemu signing.
       For a notarized release the same entitlement must survive re-signing (see afterSign note
       in scripts/bundle-qemu-mac.mjs). -->
  <key>com.apple.security.hypervisor</key>
  <true/>
</dict>
</plist>
